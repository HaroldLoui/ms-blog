server:
  port: 8888

spring:
  application:
    name: ms_blog
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3307/ms_blog?useUnicode=true&characterEncoding=UTF-8&serverTimeZone=UTC

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      table-prefix: ms_